int LED_BUILTIN = 2; //gpio 2 -- inbuilt on board - Blue colour LED
void setup() {
pinMode (LED_BUILTIN, OUTPUT);   // output mode
}
void loop() {
digitalWrite(LED_BUILTIN, HIGH);
delay(1000);
digitalWrite(LED_BUILTIN, LOW);
delay(1000);
}
